{
  "name": "test-pod",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "pnpm test",
    "lint": "pnpm --filter=@test-pod/backend --filter=@test-pod/database lint",
    "lint:fix": "pnpm --filter=@test-pod/backend --filter=@test-pod/database lint:fix",
    "format": "pnpm --filter=@test-pod/backend --filter=@test-pod/database format"
  },
  "packageManager": "pnpm@10.11.0"
}